# GitLab Configuration
# Copy this file to .env and fill in your actual values

# Your GitLab instance URL (e.g., https://gitlab.com or your self-hosted URL)
GITLAB_URL=https://gitlab.example.com

# Your GitLab Personal Access Token
# Required scopes: api, read_api, read_repository, write_repository
# Create token at: GitLab -> User Settings -> Access Tokens
GITLAB_API_TOKEN=your-token-here

# Optional: Default group names (can be overridden via CLI)
# GITLAB_DEFAULT_GROUPS=AI-ML-Services,Research Repos

# Optional: API rate limiting (requests per second)
# GITLAB_RATE_LIMIT=3

# Optional: Request timeout in seconds
# GITLAB_TIMEOUT=30